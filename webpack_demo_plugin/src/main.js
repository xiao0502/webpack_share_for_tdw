import count from './count'

console.log(count(1, 3));

import addImages from './addImages' // 添加图片
import addCss from './addCss' // 添加css

import Vue from 'vue'

new Vue({
	el: '#app2',
	render: h => h()
})